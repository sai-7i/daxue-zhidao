---
name: daxue-zhidao
description: >
  开始课程、继续学习、辅导学习或制定动态学习路线时使用的单步式教学流程：每次只教授一个
  可验证的小单元，由学习者选择下一方向并在单元边界记录进度。适用于“教我……”“继续课程”
  “从零学习……”“按步骤带我学”等请求；一次性问答、固定大纲或要求直接给完整教程时不要使用。
compatibility: opencode, codex
metadata:
  language: zh-CN
  workflow: guided-learning
---

# 大学之道

一次只教一个可验证的小单元：恢复 → 诊断 → 选择 → 定标 → 教学 → 验证 → 记录 → 暂停。
核心流程不依赖特定平台或工具名；确认运行在 OpenCode 时，按需读取
[references/opencode.md](references/opencode.md)。

## 每次会话入口

恢复课程时先停下来，不凭当前对话记忆继续：读取课程计划（若存在），检查当前检查点、下一步选择、最近单元状态、未解决问题和存储模式；再用一句话复述这些约束。
若检查点缺失、计划状态不一致或无法确认当前路线，先暂停并询问，不教学、不运行练习、不更新计划。
在 OpenCode 中，所有需要学习者作出选择、确认、授权或诊断的问题都必须调用 `question` 工具；只有工具不可用或调用失败时才用文本提问。

## 流程

1. 查找并读取已有课程计划；仅在创建、检查或更新计划时读取
   [references/course-plan-format.md](references/course-plan-format.md)。
2. 没有计划或学习者要新开课程时，必须先进行针对该主题的诊断提问，再提出起始方向。
   除主题、目标和现有水平外，只问会实际改变起点或教学方式的信息。
3. 在学习者确认课程主题和存储模式前，不创建计划。存储模式可选：当前项目、学习者指定位置、
   本次会话临时记录；无法确认时优先询问，不自行污染项目。
4. 课程依赖本机环境时，说明拟检查的项目并取得同意，再采集和保存最小必要的环境档案；
   优先实测版本，不依赖学习者回忆。无需本机环境的课程跳过此步骤。
5. 每单元给出 2–3 个一单元内可完成的方向，通常包含“继续当前路线”和针对薄弱点的方向。
   若学习者已授权默认继续，则只在真实分叉、风险或目标变化时重新询问。
6. 学习者提出切换方向时，必须先针对新方向和切换原因提问，确认新目标、可复用能力及约束；
   完成诊断并记录路线调整后，才提供新方向下的 2–3 个起始单元。默认继续授权不跳过此步骤。
7. 接受任何语义明确的选择，不要求固定口令。未选择前不进入新课、不更新计划、不运行练习。
8. 开始教学前声明本单元目标和一个可观察的成功标准，且只教这个单元所需内容。
9. 以一个小练习或一道判断题验证成功标准；先根据观察反馈，再决定单元状态。
10. 在单元边界更新计划和检查点，然后停下，由学习者选择下一步。

## 针对式提问

- 问题必须由当前主题、目标、已有观察和环境生成，不复用与主题无关的固定问卷。
- 每次需要提问前先判断问题类型：诊断、方向选择、授权、环境确认或检查点确认；不得把问题直接塞进教学正文。
- 新课程至少确认：期望成果、相关基础，以及一个最影响起点的主题特有条件。
- 切换方向至少确认：为何切换、希望保留什么，以及新方向的完成目标或限制。
- 优先一次提出 1–3 个高信息量问题；答案仍不足以决定起点时再追问，不为凑数量而提问。
- 尽量提供 2–4 个贴合上下文的选项，并允许自由回答；不要把未知信息预设为事实。
- 若课程依赖本机工具，针对式提问须确认是否允许探测，以及课程所需工具链或项目范围；
   不要求学习者手工填写可安全实测的信息。
- 在 OpenCode 中，`question` 是默认且必须使用的提问通道，不是可选的展示方式；调用前把本轮相关问题合并为一次调用，除非后续答案决定了新的问题。
- 诊断提问只用于确定路线，不能夹带新课教学或练习。收到回答后先简要复述关键约束，
  再给 2–3 个可选起始单元并等待明确选择。

## 环境档案

- 只收集会影响当前课程命令、示例或兼容性的项目：操作系统与架构、Shell、相关运行时、CLI、
  编译器、包管理器、项目清单、课程目录及其版本。
- 运行只读、低风险的版本或配置检查；新增安装、登录、升级、写配置或访问外部系统须另行询问。
- 不读取或保存密钥、令牌、密码、认证文件内容、完整环境变量、浏览器资料或无关个人信息。
  认证相关只可在确有教学需要时记录“已配置”“未配置”或“未检查”。
- 每项记录探测方式、结果和检查时间；命令不存在、执行失败或未授权时分别记为“未安装”、
  “探测失败”或“未检查”，不得猜测版本。
- 新课程首次建档；切换方向引入不同工具链、环境实际变化或版本敏感单元开始前按需刷新。
  不重复探测仍有效且与当前单元无关的信息。
- 临时模式只在会话中使用环境信息，不写入磁盘；学习者可随时要求删除或刷新已保存档案。

## 规则

- 结论须能回溯到观察。
- 仅在内容具有版本或时效敏感性、学习者要求核实，或现有信息不足时查询外部资料。
- 库、框架、SDK、API 和 CLI 优先查当前官方文档或可用的文档工具；项目内部事实优先查项目文件
  和测试；稳定基础知识无需强制联网。无法查询时明确知识边界，不声称已经核实。
- 版本敏感结论注明来源、版本和核实日期；更正显式追加，不改写历史日志。
- 只有练习观察支持成功标准时才记录“已完成”，且不扩大已确认能力的范围。
- 未达到标准时区分未通过、暂停、阻塞、跳过和取消，记录原因；下次给更小单元、补强或回炉方向。
- 你提出新方向时按同一流程接受，并执行针对式提问。
- 换主题记为路线调整：诊断并记录原因、保留能力、约束与新目标后继续。
- 练习保持本地可控；涉及外部系统先询问。
- 在 OpenCode 中按 `references/opencode.md` 使用 `question`；其他宿主没有结构化提问工具时，使用同等内容的简短文本，不因缺少特定工具而阻塞流程。
- 写计划前重新读取文件；若它在本单元期间被外部修改，保留未知修改并安全合并，无法判断时先询问。
- 临时模式不写文件；需要跨会话继续时，再经学习者确认创建计划。

## 参考

- [references/course-plan-format.md](references/course-plan-format.md)：计划文件结构、
  单元模板、选择格式。
- [references/opencode.md](references/opencode.md)：OpenCode 下的提问、资料检索和文件操作策略。
