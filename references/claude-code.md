# Claude Code 适配

仅用户 `/daxue-zhidao` 调用；`disable-model-invocation: true` 禁止自动调用。

用 `AskUserQuestion` 诊断、选方向和选课；失败即停止。用 `Read`/`Glob`/`Grep`、`Edit`/`Write`、`WebSearch`/`WebFetch` 按[课程地图格式](technology-map-format.md)和[课次地图格式](lesson-map-format.md)生成地图；只提供介绍和规划。
